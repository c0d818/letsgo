Apply an approved Stitches/OpenSpec change.

Read the active change under `openspec/changes/`, then implement it with
test-first discipline. Keep edits scoped to the approved tasks. Update the task
file as work completes and capture verification commands before the final
response.
