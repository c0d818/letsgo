Review the current Stitches change.

Check spec compliance first: does the implementation satisfy the active
OpenSpec proposal and tasks?

Then check code quality: bugs, missing tests, regressions, unsafe assumptions,
or unnecessary abstractions.

Findings should lead, ordered by severity.
