Create or update an OpenSpec change proposal.

Use the user's request and current project context to write a focused proposal
under `openspec/changes/<change-id>/` with `proposal.md`, `tasks.md`, and
`design.md` when the change spans multiple components.

Keep the proposal testable. Name the behavior that will prove the change is
done.
